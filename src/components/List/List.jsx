import React from "react";
import {ProductCard} from "../../components";
const List = () => {
  return (
    <section className="list">
      <ProductCard />
    </section>
  );
};

export default List;
